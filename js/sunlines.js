class sunlines {
	 draw(mainContext) {
		 mainContext.beginPath();
		 mainContext.fillStyle = "#ffff00";
		 mainContext.arc(0,0,100,0,2*Math.PI,false);
		 mainContext.fill();
		 mainContext.stroke();
}
}